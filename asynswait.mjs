// PROMISE

//A promise iS a promisee of a code execution.
// the code either execute or fails.

// Syntax :

// let Promise = new Promise(function(resolve, reject){});

// resolve and reject are two callbacks provided by javascript
// resolve: if the job is finished successfully.
// reject: it the job is fails

// async await

// the code in the try block will execute first and if it throws an exception the code in the catch block will be executed.

try {
} catch (error) {}
