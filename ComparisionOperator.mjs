// comparision operator

// > : greater than
// < : less than
// >= :greater than equal too
//  <= : less than equal too
// == : double equal to or loosely equality operator
// === : triple equal to od strictily equaity operator
// =! : not equal to

console.log(8 <= 2);
console.log(2 == "2"); // it checks data only
console.log(2 === "2"); // it checks data and its type also
console.log(!true);

console.log(1 != "1");
console.log(1 != false);
